# rekord-api — Decisions & Assumptions Log

Every assumption, deviation, or judgment call the agent makes goes here — newest first.

| Date | Phase | Decision | Why | Spec ref |
|---|---|---|---|---|
| 2026-07-18 | Setup | Zero-infrastructure mode: PGlite is the only database until deploy; no servers started during the build | Developer machine has Node + npm only | SPEC §1, §9 |
| 2026-07-18 | Setup | Splits stored as jsonb on race_results | Only ever read with their result; avoids a join for no query benefit | SPEC §6 |
| 2026-07-18 | Setup | FakeProvider is the default integration provider (PROVIDERS=fake) | Strava/Garmin credentials unavailable during build; flow must still be fully testable | SPEC §7 |
| 2026-07-18 | Bootstrap | Developer machine runs Node v26.3.1, newer than the fixed Node 22 LTS target; proceeded without downgrading | All tooling (Hono, Drizzle, Vitest, tsx) installed and ran cleanly; `package.json` still pins `engines.node: >=22` so CI/deploy stay on the intended LTS line | SPEC §2 |
| 2026-07-18 | Bootstrap | `tsconfig.json` includes both `src` and `tests` with `noEmit: true`; a separate `tsconfig.build.json` (extends base, `include: ["src"]`, `noEmit: false`) backs the `build` script | Keeps test code type-checked without emitting test files into `dist/` | SPEC §4 |
| 2026-07-18 | Phase 0 | AppError → HTTP status mapping: UNAUTHORIZED 401, FORBIDDEN 403, NOT_FOUND 404, VALIDATION_FAILED 400, CONFLICT 409, RATE_LIMITED 429, INTERNAL 500 | APP_SPEC §10.1 fixes the error *body* shape (`code`/`message`/`fieldErrors`) but not per-code HTTP statuses; chose the conventional REST mapping | APP_SPEC §10.1 |
| 2026-07-18 | Phase 0 | `env.ts` gives `DATABASE_URL`/`JWT_SECRET`/`APP_ORIGINS`/`PROVIDERS` dev-safe defaults instead of failing when unset | Zero-infrastructure mode: `npm test`/`npm run build` must run with no `.env` file present; real values only matter once Neon/JWT signing/CORS are wired in later phases | SPEC §1, §2 |
| 2026-07-18 | Phase 0 | pino logging folded into `middleware/request-id.ts` (logs on the way out, correlated by the generated/propagated request id) rather than a separate logging middleware file | SPEC §4 folder structure lists only `request-id.ts` among middleware for this concern, not a distinct logging file | SPEC §4 |
| 2026-07-18 | Phase 0 | Added `envelopeSchema(dataSchema)` + `ErrorEnvelopeSchema` to `lib/envelope.ts` (Zod builders wrapping `{data, meta?}` / `{code, message, fieldErrors?}`) for every future module's OpenAPI response schemas, instead of each module hand-rolling its own wrapper | Keeps the OpenAPI spec's response shapes byte-identical to the runtime envelope everywhere, per the "envelope … everywhere" hard rule | SPEC §3, CLAUDE.md hard rules |
| 2026-07-18 | Phase 0 | `OpenAPIHono`'s `defaultHook` and `middleware/error-handler.ts`'s `ZodError` branch both call a single shared `validationErrorBody()` (in `lib/app-error.ts`) to build the `{code, message, fieldErrors}` body | Route-level Zod validation failures (query/params/body via `createRoute`) and deeper thrown `ZodError`s must produce byte-identical envelopes; an adversarial review caught the two call sites duplicating the object construction instead of sharing one function | SPEC §9 (contract checks) |
| 2026-07-18 | Phase 0 | `/openapi.json` `info.version` is hardcoded `"0.1.0"` rather than read from `package.json` | Reading `package.json` at runtime adds JSON-import complexity for no benefit yet; revisit if this drifts from the real package version | SPEC §4 |
| 2026-07-18 | Phase 0 | Fixed a bug an adversarial review caught: `app.doc()`'s `servers` entry was `[{url: '/api/v1'}]` while every `createRoute` path already bakes in the full `/api/v1/...` prefix (per SPEC §5's own naming example), so spec-compliant consumers (Scalar's "Try it", codegen, Postman) would resolve `server.url + path` to the doubled `/api/v1/api/v1/health` and 404. Changed `servers` to `[{url: '/'}]` since paths are already absolute; added a regression test in `tests/openapi.test.ts` that resolves `servers.url + path` and requests it | Every future Phase 3+ route follows the same absolute-path convention, so this would have silently broken interactive docs/codegen for the whole API, not just health | APP_SPEC §10.1 |
| 2026-07-18 | Phase 1 | Added `db/schema/enums.ts` and `db/schema/uploads.ts`, beyond the illustrative file list in SPEC §4 | `enums.ts` avoids circular imports for enums shared across tables (e.g. `terrain` used by both `events` and `race_results`); `uploads.ts` gives the `uploads` table (SPEC §6) a home since no file owns it in the sample tree — `db-diagram.drawio` already anticipated an uploads table | SPEC §4 |
| 2026-07-18 | Phase 1 | PG enum value naming: `distance_category` uses snake_case (`five_k`, `ten_k`, `half_marathon`, …) even though APP_SPEC §9's Dart enum is camelCase (`fiveK`, `tenK`, …); added a `gender` enum (`male`/`female`) not explicitly named in SPEC §6 | SPEC §5 fixes "PG enums: snake_case name, lowercase values" for the DB layer; the API layer's wire-format string values (Phase 3+ Zod schemas) are a separate, later decision. `gender` needs a concrete enum for the WMA age-grading lookup (APP_SPEC §7.7), which is binary male/female | SPEC §5, §6 |
| 2026-07-18 | Phase 1 | `claims.status` and `edit_requests.status` share one `review_status` enum (`pending`/`approved`/`rejected`) instead of two separate enum types | Both are the same submit → admin-review shape (APP_SPEC §7.13: "apply or reject"); SPEC §6 lists `status enum` for both without naming values | SPEC §6 |
| 2026-07-18 | Phase 1 | `goals` has no `status`/`progress` column | SPEC §6 goals row explicitly notes "progress computed at read time from results" — persisting a status would drift from the source-of-truth results | SPEC §6 |
| 2026-07-18 | Phase 1 | `integration_status` enum values: `connected`/`disconnected`/`error` | SPEC §6 lists integrations.`status` with no enum values named (unlike `provider enum(strava/garmin)` on the same row); chose the minimal set covering APP_SPEC §7.11's connect/sync/disconnect flow plus a failure state | SPEC §6, APP_SPEC §7.11 |
| 2026-07-18 | Phase 1 | `runners.display_name` and `events.name` get a plain btree index for now (`// TODO(rekord-api)` in the schema files), not the `pg_trgm` GIN index SPEC §6 calls for | PGlite supports `pg_trgm` via a loadable contrib module, but wiring the extension (custom migration + test-harness changes) is only worth doing once Phase 3's actual search/suggest queries need it and can verify similarity-search behavior; upgrading the index later doesn't touch existing data | SPEC §6, §10 (Phase 3) |
| 2026-07-18 | Phase 1 | Fixed a bug caught before the adversarial review even ran: the partial unique index on `claims` (`WHERE status = 'pending'`) was built with `eq(t.status, 'pending')`, which drizzle-kit compiled into a migration with a bound `$1` placeholder inside a static `CREATE INDEX ... WHERE` DDL statement — invalid SQL with no parameter to bind at migration time. Rewrote as `sql\`${t.status} = 'pending'\`` so the literal is inlined; regenerated the migration and confirmed the literal appears (`WHERE "claims"."status" = 'pending'`), with a test that proves the partial index actually enforces one pending claim per (user, runner) | `eq()`/other query-builder helpers produce parameterized SQL meant for runtime queries, not static DDL — any `.where()` on an index/check constraint must use a raw `sql` literal | SPEC §6 |
| 2026-07-18 | Phase 1 | `db/client.ts` selects Neon (`drizzle-orm/neon-http`) only when `NODE_ENV==='production'`; otherwise a fresh in-memory PGlite is created per process and auto-migrated via an async `migrateDb()` that `index.ts` awaits before `serve()` starts. Production migrations stay a deliberate deploy-time step (SPEC §13), never automatic | Matches SPEC §2's "PGlite (in-memory) for all tests **and local verification**" — zero-infra dev shouldn't need a manual migrate step. Known limitation logged here: since PGlite is in-memory and per-process, running `npm run dev` and `npm run db:seed` as two separate real processes won't share data — a real Neon dev database is the fix once the developer needs persistent local state | SPEC §2, §4, §13 |
| 2026-07-18 | Phase 1 | After an adversarial review flagged the repeated `uuid().primaryKey().defaultRandom()` (16×) and `timestamp({withTimezone:true}).notNull().defaultNow()` (10×) boilerplate across schema files as a drift risk, extracted `id()`/`timestamptzNow()` into `db/schema/columns.ts` and updated all 16 tables to use them; also extracted `db/pglite.ts` (`createPgliteDb()`/`migratePgliteDb()`/`MIGRATIONS_FOLDER`) so `db/client.ts` and `tests/helpers/createTestApp.ts` share one PGlite-construction path instead of duplicating it | Confirmed via `drizzle-kit generate` that the refactor produced zero schema diff (byte-identical DDL) before committing; the pre-refactor duplication was the exact kind of thing that silently drifts when only some call sites get updated | SPEC §3 (reuse) |
| 2026-07-18 | Phase 1 | `db/seed/prng.ts` implements a mulberry32 seeded PRNG (fixed seed `20260718`); nothing in the seed path calls `Math.random()`, `Date.now()`, or `new Date()` — the reference "today" for editions/results is the fixed year pool `[2021..2025]` | SPEC §8: "must produce the same dataset every run (fixed RNG seed)". Anchoring years in the past also means seeded race dates are never accidentally "in the future" relative to whenever the seed actually runs | SPEC §8 |
| 2026-07-18 | Phase 1 | "Deterministic" is interpreted as *reproducible structure/counts/relationships* (same runner names, same event slugs, same row counts every run — asserted in `tests/db/seed.test.ts` by running the seed twice against separate PGlite instances), not byte-identical primary keys or password-hash strings | Postgres's `gen_random_uuid()` (used by every table's `id()` default) is never seeded by the app's PRNG, so UUIDs always differ run-to-run regardless; `argon2.hash()` also randomizes its salt per call by design — forcing a fixed salt would be a security anti-pattern even in seed data. Both are irrelevant to the counts/content the app and its tests actually depend on | SPEC §8 |
| 2026-07-18 | Phase 1 | Seeded 3 "showcase" runners get claims: runner #0 is pre-claimed+verified with an `approved` claim (backs `runner@rekord.ph`); #1 and #2 stay unclaimed with a `pending` claim each, linked to two throwaway users (`ana.reyes@example.com`, `miguel.santos@example.com`) | SPEC §8 requires "pending claims" (plural) plus the three named role accounts; approving a claim is a Phase 5 transaction this seed doesn't run, so #1/#2 intentionally stay `isClaimed:false` until a real approval happens | SPEC §8 |
| 2026-07-18 | Phase 1 | Seeded `integrations.accessTokenEnc`/`refreshTokenEnc` are plaintext-looking placeholder strings (`placeholder-access-<hex>`), not real AES-GCM ciphertext; `api_keys.keyHash` uses a plain SHA-256 hex digest of a PRNG-generated secret, not argon2 | `lib/crypto.ts` (AES-GCM for provider tokens) doesn't exist until Phase 6; API keys are long random secrets verified on every organizer bulk-upload request, so a fast hash (SHA-256) is the right primitive — argon2's deliberate slowness is for low-entropy user passwords, not high-entropy keys. Phase 6/7 must encrypt/hash real values the same way when those modules land | SPEC §6, Phase 6/7 |
| 2026-07-18 | Phase 1 | Race result ranks (`overallRank`/`genderRank`/`ageGroupRank`/`fieldSize`) are computed in the seed script itself by grouping generated results per (edition, distanceCategory), sorting by `officialTimeMs`, and padding `fieldSize` with a random surplus — not left as random unrelated numbers | SPEC §8 asks for "realistic … ranks and field sizes"; leaving ranks uncorrelated with actual times would make the seeded leaderboards (Phase 3) nonsensical | SPEC §8 |
| 2026-07-18 | Phase 1 | An adversarial review caught two real bugs, both fixed and verified empirically against a fresh seed run: (1) `generateSplits()` used a fixed 5000m step, so a `five_k` result (distanceMeters=5000) always produced an empty `[]` — stored as non-null "has splits" but with zero checkpoints; fixed by stepping 1K for ≤5K distances and 5K otherwise, plus a call-site guard that stores `null` if the generated array is ever empty. (2) result generation picked a runner + edition + distance independently per row with no de-duplication, letting the same runner "finish" the same race/distance twice; fixed with a bounded retry loop (10 attempts) against a `Set` of used (runner, edition, distance) combos before falling back | Verified via a one-off script against `runSeed()`: 0 empty-but-non-null splits arrays, splits ratio 39.4% (target "~40%"), 0 duplicate combos across all 815 results | SPEC §8 |
| 2026-07-18 | Phase 1 | Simplification cleanup from the same review: extracted `firstOrThrow()` (replacing 7 repeated `if (!row) throw ...` guards after `.returning()`) and `randomDateString()` (replacing duplicated month/day padding logic for editions and import candidates); batched the 5 role/showcase user inserts into one `.values([...])` call correlated back by email instead of 5 sequential inserts; removed the unused `pickMultiple()` export from `prng.ts`; replaced the 200-row chunked `race_results` insert loop with a single bulk insert (815 rows × ~20 columns is far under Postgres's per-statement parameter limit, so chunking was unneeded complexity) | None of these changed the seeded data (row counts/content identical before and after) — pure reuse/simplification | SPEC §3 (reuse) |
